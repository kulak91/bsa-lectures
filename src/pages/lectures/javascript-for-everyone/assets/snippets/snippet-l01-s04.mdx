```js
var message;
message = 'Hello World';

var name = 'John',
    age = 24;

var count = 10;
if (count > 5) {
  var count = 5; // Not declares a new variable, but using existing hoisted count
}
// count is equal to 5 😰
```
